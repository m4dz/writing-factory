# Tasks: hygiene

- [x] `experiments/runs/` with 15 directories and manifests; journal, grids,
      reports moved; duplicates, logs and archived draws removed.
- [x] Tooling defaults (`journal_des_murs`, `run_s4`, `run_ch2`, `run_scene`)
      and `.gitignore` follow the move.
- [x] Chapter 7 briefs to `chapters/07-anniversaire/`; `ch7.py` paths updated;
      snapshots identical.
- [x] `docs/doctrines.md`, `docs/architecture.md`, `docs/runbook.md`.
- [x] ADRs 0006–0020 and the index.
- [x] `README.md` rewritten; `CLAUDE.md` ≤ 60 lines.
- [x] `openspec/specs/` for api, pipeline, chapter-spec, retrieval, eval,
      roleplay, render.
- [x] Superseded documents deleted: old RUNBOOK, handover, deck contract, SSE
      front spec, pipeline doc, movement note, tooling READMEs.
- [x] OpenWebUI out of compose; `query_test.py` default collection fixed.
- [x] Tests: `test_lint_snapshot` sources repointed; no test rewritten or
      deleted in this step.
- [x] `make check` green; grep for removed paths clean.
