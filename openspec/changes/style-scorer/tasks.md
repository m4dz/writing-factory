# Tasks: style-scorer

- [x] `factory.eval.style`: marks, `features`, `score`, `report`, `main`.
- [x] `factory eval style` in the CLI dispatch.
- [x] Tie-break in both best-of selections of `write_node`;
      `_style_tiebreak_note`.
- [x] `tests/unit/test_style_score.py`: étalons vs generic drafts and journal
      mean; genre vs dry; body cleaning; tie-break order and reach.
- [x] Docs: architecture (eval, write node), runbook (bench section names
      `factory eval style`), plan status.
- [x] Snapshots unchanged (no prompt moves).
