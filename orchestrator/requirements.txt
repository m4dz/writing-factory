langgraph>=1.0
chromadb-client>=0.5

# Rendu en voix clonée (Qwen3-TTS via MLX), étape finale du pipeline de démo.
# Dans le MÊME venv que l'orchestrateur, et non dans celui du dépôt TTS voisin :
# mlx-audio déclare `Requires-Python: >=3.10` et fonctionne sur notre 3.10.9
# (vérifié à l'import). Le `python3.11` du RUNBOOK TTS est un choix
# d'installation, pas une contrainte. Un seul venv à recréer le jour J.
# ~542 MB de paquets (mlx, transformers, scipy) — pas de torch.
mlx-audio>=0.4
soundfile>=0.12
numpy>=1.26
